# Copy this file to .env and fill in real values. Never commit .env itself.

PORT=4000

# Signs the Platform's own login session cookie (independent of Directus's
# auth and Camunda's candidate groups — see platform/src/users.js). Any
# random string; generate a fresh one per environment.
SESSION_SECRET=generate-your-own-random-string

DIRECTUS_URL=http://localhost:8055
# Must match ADMIN_EMAIL / ADMIN_PASSWORD in directus/api/.env — the
# Platform BFF authenticates as this account to call Directus's REST API.
DIRECTUS_SERVICE_EMAIL=admin@example.com
DIRECTUS_SERVICE_PASSWORD=change-me

CAMUNDA_URL=http://localhost:8080/engine-rest
